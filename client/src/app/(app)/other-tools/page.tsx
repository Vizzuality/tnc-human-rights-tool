import { Metadata } from "next";

import OtherTools from "@/containers/other-tools";

export const metadata: Metadata = {
  title: "Other Tools | Human Rights Screening Tool",
  description: "",
};

export default function OtherToolsPage() {
  return <OtherTools />;
}
